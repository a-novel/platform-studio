/** Localized validation messages consumed by server-side authentication forms. */
export interface AuthValidationMessages {
  confirmPassword: string;
  currentPassword: string;
  email: string;
  newPassword: string;
  password: string;
  passwordMismatch: string;
}
